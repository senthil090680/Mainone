<%= form_for(@posm_type) do |f| %>
  <% if @posm_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@posm_type.errors.count, "error") %> prohibited this posm_type from being saved:</h2>

      <ul>
      <% @posm_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
