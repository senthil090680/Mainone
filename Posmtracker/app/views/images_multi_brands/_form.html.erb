<%= form_for(@images_multi_brand) do |f| %>
  <% if @images_multi_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@images_multi_brand.errors.count, "error") %> prohibited this images_multi_brand from being saved:</h2>

      <ul>
      <% @images_multi_brand.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
