<%= form_for(@brand_detail) do |f| %>
  <% if @brand_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brand_detail.errors.count, "error") %> prohibited this brand_detail from being saved:</h2>

      <ul>
      <% @brand_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
