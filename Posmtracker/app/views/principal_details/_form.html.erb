<%= form_for(@principal_detail) do |f| %>
  <% if @principal_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@principal_detail.errors.count, "error") %> prohibited this principal_detail from being saved:</h2>

      <ul>
      <% @principal_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
