<h1>Listing principal_details</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @principal_details.each do |principal_detail| %>
  <tr>
    <td><%= link_to 'Show', principal_detail %></td>
    <td><%= link_to 'Edit', edit_principal_detail_path(principal_detail) %></td>
    <td><%= link_to 'Destroy', principal_detail, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Principal detail', new_principal_detail_path %>
