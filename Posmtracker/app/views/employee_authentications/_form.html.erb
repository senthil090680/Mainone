<%= form_for(@employee_authentication) do |f| %>
  <% if @employee_authentication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_authentication.errors.count, "error") %> prohibited this employee_authentication from being saved:</h2>

      <ul>
      <% @employee_authentication.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
