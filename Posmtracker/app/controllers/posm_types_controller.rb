class PosmTypesController < ApplicationController
 
 
 def get_brand_details
#     
    # brand_id = params['brand_id']
#     
    # if !brand_id.nil? && !brand_type_id.nil? && brand_type_id != "" && brand_id != ""
#       
                # branch_type = BrandType.find(brand_type_id)
                # branch_name = Brand.find(brand_id)
# 
            # json = {"success" => 1, "message" => "success" ,"brand_type_name" => branch_type.type1, "brand_name" => branch_name.name}
    # else
            # json = {"success" => 0, "message" => "Required field's missing"}
    # end
    # render :json => json
  end
  
 
end
