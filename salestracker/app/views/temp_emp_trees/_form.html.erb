<%= form_for(@temp_emp_tree) do |f| %>
  <% if @temp_emp_tree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@temp_emp_tree.errors.count, "error") %> prohibited this temp_emp_tree from being saved:</h2>

      <ul>
      <% @temp_emp_tree.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
